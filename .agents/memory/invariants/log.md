# Invariants Update Log

## 2026-09-11
* **Creation**: Established `.agents/memory/invariants/` catalog under OKF v0.2 per Proposal `P-008`. Initialized permanent invariants `INV-001` (BigInt epoch schema), `INV-002` (Caddy network isolation), `INV-003` (SDK centralization), `INV-004` (WASM exit flush handshake), and `INV-005` (Platform trust boundary).
